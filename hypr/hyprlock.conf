$accent = $mauve
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

# GENERAL
general {
  disable_loading_bar = true
  hide_cursor = true
}

# BACKGROUND
background {
    monitor =
    #path = screenshot
    path = $HOME/.config/hypr/arch-black-4k.png
    #color = $background
    blur_passes = 1
    #contrast = 1
    #brightness = 0.5
    #vibrancy = 0.2
    #vibrancy_darkness = 0.2
}

# # LAYOUT
# label {
#   monitor =
#   text = Layout: $LAYOUT
#   color = $text
#   font_size = 25
#   font_family = $font
#   position = 30, -30
#   halign = left
#   valign = top
# }

# TIME
label {
  monitor =
  text = $TIME
  color = $text
  font_size = 90
  font_family = JetBrains Mono Nerd
  position = 0, 350
  halign = center
  valign = center
}

# DATE
label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $text
  font_size = 25
  font_family = JetBrains Mono Nerd
  position = 0, 800
  halign = center
  valign = cneter
}

# USER AVATAR
# image {
#   monitor =
#   path = $HOME/.face
#   size = 100
#   border_color = $accent
#   position = 0, 75
#   halign = center
#   valign = center
# }


# # INPUT FIELD
# input-field {
#   monitor =
#   size = 300, 60
#   outline_thickness = 4
#   dots_size = 0.2
#   dots_spacing = 0.2
#   dots_center = true
#   outer_color = $accent
#   inner_color = $surface0
#   font_color = $text
#   fade_on_empty = false
#   placeholder_text = <span foreground="##$textAlpha"><i>󰌾 Logged in as </i><span foreground="##$accentAlpha">$USER</span></span>
#   hide_input = false
#   check_color = $accent
#   fail_color = $red
#   fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
#   capslock_color = $yellow
#   position = 0, -47
#   halign = center
#   valign = center
# }
# INPUT FIELD
input-field {
    monitor =
    size = 250, 60
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = $accent
    inner_color = $surface0
    font_color = $text
    fade_on_empty = false
    rounding = -1
    check_color = rgb(204, 136, 34)
    placeholder_text = <i><span foreground="##0D1117">Input Password...</span></i>
    hide_input = false
    position = 0, -200
    halign = center
    valign = center
    }
